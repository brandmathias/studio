
{
  "build": {
    "env": {
      "VERCEL_BUILD_ARGS": "--skip-stats --skip-framework-detection"
    }
  },
  "rewrites": [
    {
      "source": "/genkit/:path*",
      "destination": "/api/genkit/:path*"
    }
  ]
}
